mutation SplitTeam($eventId: ID!, $roundNo: ID!) {
  splitTeam(eventId: $eventId, roundNo: $roundNo) {
    ... on Error {
      __typename
      message
    }
    ... on MutationSplitTeamSuccess {
      __typename
      data {
        name
        roundNo
        leaderId
      }
    }
  }
}
